## Database Setup

I created a SQL Server database called hospital to store the hospital admissions data for this project.

The database will be used to separate the raw admission data from the curated data that will be produced after the data quality and deduplication steps.

#### SQL Script:

1. Creates the `hospital` database.
2. Switches the SQL session to the `hospital` database using `USE`.

